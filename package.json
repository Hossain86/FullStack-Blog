{
  "scripts": {
    "dev": "concurrently \"npm --prefix backend run dev\" \"wait-on http://localhost:5173 && npm --prefix frontend run dev\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "wait-on": "^8.0.3"
  },
  "dependencies": {
    "react-icons": "^5.5.0"
  }
}
